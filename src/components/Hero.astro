---
import { siteConfig } from '../config';
---

<header class="hero">
  <h1>{siteConfig.name}</h1>
  <p class="tagline">{siteConfig.tagline}</p>
</header>

<section class="bio">
  <p>
    German, spent some time in Switzerland, now based in Glasgow. Just wrapped up my master's and building enterprise cloud infrastructure at <a href="https://www.jpmorganchase.com/" target="_blank" rel="noopener noreferrer" class="company-link">JPMorgan Chase</a>. Currently deep-diving into Kubernetes. Off the clock, I'm either at hackathons building random stuff, on the golf course, or on a mission to find Glasgow's best coffee (<a href="/coffee" class="coffee-link">see rankings</a>).
  </p>
</section>

<style>
  .hero {
    text-align: center;
  }

  h1 {
    font-size: 2em;
    font-weight: 400;
    margin-bottom: 0.5em;
    color: var(--theme-text);
    letter-spacing: -0.02em;
  }

  .tagline {
    color: var(--theme-sub-text);
    font-size: 0.9em;
  }

  .bio {
    display: flex;
    flex-direction: column;
    gap: 1em;
    line-height: 1.8;
  }

  .bio p {
    margin: 0;
  }

  .company-link,
  .coffee-link {
    color: var(--theme-accent);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }

  .company-link:hover,
  .coffee-link:hover {
    background: none;
    border-bottom-color: var(--theme-accent);
  }

  @media only screen and (max-width: 768px) {
    h1 {
      font-size: 1.6em;
    }
  }
</style>
